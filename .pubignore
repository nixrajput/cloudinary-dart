# This file REPLACES .gitignore when publishing, so the build-artifact
# excludes are repeated here rather than inherited. Without them, pub ships
# coverage/ and .dart_tool/ to every consumer.
.dart_tool/
build/
coverage/
pubspec.lock
.idea/
.claude/
.githooks/
.playwright-mcp/

# Development-only; useful in the repo, noise in the package.
AGENTS.md
CLAUDE.md
cspell.json

# Unreferenced fixture carried over from 1.x.
assets/test.png

# Example platform scaffolding. pub.dev renders the example from its Dart
# sources, so the Xcode, Gradle and web shells are dead weight.
example/android/
example/ios/
example/macos/
example/linux/
example/windows/
example/web/
example/.dart_tool/
example/build/
example/pubspec.lock
example/.metadata
