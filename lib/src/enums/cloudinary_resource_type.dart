/// CloudinaryResponseType enum is used to specify the type of the resource
enum CloudinaryResourceType {
  /// The [image] type
  image,

  /// The [raw] type
  raw,

  /// The [video] type
  video,

  /// The [auto] type
  auto,
}
